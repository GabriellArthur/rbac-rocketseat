{
  "name": "next-saas-rbac",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -e packages/env/.env -- turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*",
    "config/*"
  ]
}
